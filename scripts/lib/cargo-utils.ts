/**
 * @deprecated Import from `src/lib/cargo-utils` in app code.
 * Scripts re-export the same implementation to evitar duas fontes de verdade.
 */
export { canonicalCargo } from "../../src/lib/cargo-utils"
